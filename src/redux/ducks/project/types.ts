export type TagsItem = {
  id: number;
  mass: string;
};

// export type Filter = { namePage: string; mass: FilterItem[] };

export type ProjectItems = {
  id: number;
  sort: number;
  structure: string;
  company_id: string;
  name: string;
  qr: string;
};
